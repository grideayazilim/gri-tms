{
  "name": "timesheet-management-system",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "scripts": {
    "client:dev": "npm run dev -w client",
    "server:dev": "npm run dev -w server",
    "shared:build": "npm run build -w shared",
    "shared:dev": "npm run dev -w shared"
  }
}
